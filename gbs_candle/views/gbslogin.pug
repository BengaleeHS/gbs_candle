doctype html
html
  head
    meta(charset='UTF-8')
    title GBS Candle | Login
    style(type='text/css').
      body{text-align:center;}
      input:hover, input:active, button:active, button:hover{box-shadow:0px 0px 5px 0.5px lightgray}
      a:hover, a:visited, a:link{text-decoration:none}
      button{
        margin:0.5em;
        margin-left:0em;
        border-radius: 0 1em 1em 0;
        height:5.4em;
        outline-width: 3px;
        border: 3px solid orange;
        background-color: white;
      }
      input{
        text-align:center;
        padding:0;
        border-radius: 1em;
        border : 3px solid orange;
        height: 2em;
        margin: 0.5em;
        width: 25em;
        display:block;
      }
      input[name='uid']{border-radius: 1em 0 0 0;}
      input[name='pwd']{border-radius: 0 0 0 1em;}
      form.login{
        text-align:center;
        margin:auto;
        padding: 1em;
      }
      div{
        display: inline-block;
        test-align: center;
      }
      h1,h2,h3,h4{text-align: center;}
  body
    div(style='border: 1px solid white; box-shadow:0 1px 5px 1px gray;position:absolute;top:50%;left:50%;margin-left:-230px;margin-top:-180px;')
      div(style="height=10%")
        a(href="http://gbs.hs.kr")
          h3(style='margin-bottom:0px;color:orange') GBS
        h1(style='margin-top:0px; margin-bottom:15px;color:orange') Candle
      form.login(method='post', action='/',name='loginform')
        div.loginform
          div
            input.login(name='uid',type='text',placeholder='학교 아이디')
            input.login(name='pwd',type='password', placeholder='비밀번호')
          div(style='float:right;')
            button.login(type='submit', style="display:inline-block; ") 
              b 로그인
      div
        p(style="font-size:15px;color:lightgray;margin-top:0px;margin-bottom:25px") By BengaleeHS